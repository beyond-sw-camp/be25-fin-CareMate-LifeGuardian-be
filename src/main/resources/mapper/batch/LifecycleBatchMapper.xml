<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.caremate.lifeguardian.lifecycle.mapper.LifecycleBatchMapper">

    <select id="selectLifecycleTargets"
            resultType="com.caremate.lifeguardian.lifecycle.dto.internal.LifecycleTargetDto">
        SELECT pc.id AS potentialCustomerId
        FROM potential_customer pc
        WHERE pc.birth_date IS NOT NULL
          AND pc.is_active = FALSE
          AND TIMESTAMPDIFF(YEAR, pc.birth_date, CURDATE()) >= 21
    </select>

    <update id="graduatePotentialCustomer"
            parameterType="com.caremate.lifeguardian.lifecycle.dto.internal.LifecycleTargetDto">
        UPDATE potential_customer
        SET is_active = TRUE,
            updated_at = NOW()
        WHERE id = #{potentialCustomerId}
          AND is_active = FALSE
    </update>

</mapper>
